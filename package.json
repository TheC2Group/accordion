{
  "name": "c2-accordion",
  "version": "2.5.0",
  "description": "Accessible accordion using CSS transition to expand and contract.",
  "main": "accordion.js",
  "scripts": {
    "standalone": "browserify accordion.js --outfile standalone/accordion.js --standalone Accordion --exclude jquery",
    "dist": "uglifyjs standalone/accordion.js --output standalone/accordion.min.js --comments",
    "build": "npm run standalone && npm run dist"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:TheC2Group/accordion.git"
  },
  "keywords": [
    "accordion"
  ],
  "author": "The C2 Group",
  "license": "MIT",
  "dependencies": {
    "jquery": "^2.1.4"
  }
}
